import 'package:flutter/material.dart';
import 'main_app.dart';
import 'interface_app_top_tabbar.dart';

// main basically just runs the app
// creates our app as an object and runs it
void main() => runApp(InterfaceApp());

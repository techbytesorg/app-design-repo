import 'package:flutter/material.dart';
import 'data.dart';
import 'main_app.dart';

// main basically just runs the app
// creates our app as an object and runs it
void main() => runApp(InterfaceApp(daysOfWeek, todayIndex, things));
